/* eslint no-shadow: ["error", { "allow": ["state"] }] */

/**
 * @description 主要的store
 */

const state = {
};

const mutations = {
};

const actions = {
};

export default {
    state,
    mutations,
    actions,
};
